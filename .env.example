# Backend API base URL. Vite bakes this into the static build at build time
# (not read at runtime), so it must be set on the Railway service as a
# variable named exactly VITE_API_BASE_URL - the Dockerfile declares it as a
# build ARG so Railway passes it through to the build automatically.
VITE_API_BASE_URL=https://api.academy-hub.net
